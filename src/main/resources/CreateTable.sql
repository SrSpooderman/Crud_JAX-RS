CREATE TABLE Reservas (
    locationNumber VARCHAR(255) PRIMARY KEY,
    client VARCHAR(255),
    agency VARCHAR(255),
    price VARCHAR(255),
    roomType VARCHAR(255),
    hotel VARCHAR(255),
    checkIn VARCHAR(255),
    roomNights VARCHAR(255)
);